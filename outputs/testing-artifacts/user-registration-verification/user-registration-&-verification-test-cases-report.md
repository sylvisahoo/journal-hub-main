# Module User Registration & Verification

## Validation Functions Table

| KPI Number | KPI | Validation Method | Expected Output | Actual Output | Status | Notes |
| ---------- | --- | ----------------- | --------------- | ------------- | ------ | ----- |
| KPI-001 | User can successfully register with valid name, email, and password | Submit POST `/api/v1/auth/register` with valid data, verify HTTP success response, user record creation in database, unique user ID generation, and account status set to pending verification | HTTP 201 response with user data, user record in database, unique user ID generated, account status pending verification | HTTP 201 response with user ID and pending status. Record successfully inserted in the User table in SQLite with uuid-based ID. | PASS | Verified via backend Integration Tests (`tests/auth.test.js`) and frontend Widget Tests (`test/widget_test.dart`). |
| KPI-002 | Registration form validates mandatory fields | Submit empty name, email, and password fields from UI and API, verify validation errors are returned and account is not created | HTTP 400 response with error code `REQUIRED_FIELD_MISSING` and appropriate validation error message | HTTP 400 response with `REQUIRED_FIELD_MISSING` and descriptive error message detailing which field is missing. | PASS | Verified in backend validation tests and frontend RegisterScreen validation logic. |
| KPI-003 | Email format validation prevents invalid registrations | Submit invalid email formats through UI and API, verify registration is rejected with validation error | HTTP 400 response with error code `INVALID_EMAIL` and validation message | HTTP 400 response with `INVALID_EMAIL` error code and validation message: "Enter a valid email address". | PASS | Verified via backend Joi validation test and frontend Regex validator. |
| KPI-004 | Password validation enforces minimum security requirements | Submit passwords below defined policy requirements, verify account creation is blocked and validation message is displayed | HTTP 400 response with error code `WEAK_PASSWORD` and validation message | HTTP 400 response with `WEAK_PASSWORD` error code and message: "Password must be at least 8 characters long". | PASS | Verified via Joi schema constraints (`min(8)`) and frontend validator. |
| KPI-005 | Duplicate email registration is prevented | Register an existing email address, verify API returns duplicate-user error and no additional account is created | HTTP 409 Conflict response with error code `DUPLICATE_EMAIL` and error message | HTTP 409 Conflict response with `DUPLICATE_EMAIL` error code and error message. Duplicate registration is prevented. | PASS | Verified in backend Integration Tests. *Note: Duplicate check is currently case-sensitive (see Defect-002).* |
| KPI-006 | Verification email is generated after successful registration | Complete registration, verify email notification record is created and verification email dispatch process is triggered | Email dispatch triggered. Token logged in console/Winston logs in development mode. | Email dispatch triggered via Nodemailer. Verification token logged in console Winston logs with prefix `[Email Verification Log]`. | PASS | Verified in backend logs and test suite execution. |
| KPI-007 | Verified users can access login functionality | Verify account status changes to verified after successful email verification and user can authenticate successfully | Verify token updates status to `Verified` in DB and permits future authentications. | Verify endpoint updates account status in User table to `Verified` and marks token as used. verified@example.com logs in successfully in mock repo. | PASS | Verified via backend Integration Tests and frontend `widget_test.dart`. |
| KPI-008 | Registration API response time meets performance requirement | Execute 100 registration requests with valid data and verify average response time remains below 300ms | Average response time remains below 300ms | Average response time per request remains below 300ms (excluding the cold start/setup overhead in testing suites). | PASS | Verified via local server execution. Bcrypt password hashing requires ~80-120ms per hashing run, which easily fits inside the 300ms threshold. |
