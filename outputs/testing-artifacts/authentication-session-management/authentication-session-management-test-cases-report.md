# Module Authentication & Session Management

## Validation Functions Table

| KPI Number | KPI | Validation Method | Expected Output | Actual Output | Status | Notes |
| ---------- | --- | ----------------- | --------------- | ------------- | ------ | ----- |
| KPI-009 | User can log in with valid credentials | Submit valid email and password to POST `/api/v1/auth/login`, verify access token and refresh token are generated | HTTP 200 response with `accessToken`, `refreshToken`, and `expiresAt` fields. Active session recorded in `UserSession` table. | HTTP 200 response containing `accessToken`, `refreshToken`, and `expiresAt`. SQLite record added to `UserSession` table with `is_active = 1` and `last_login_at` updated in `User` table. | PASS | Verified via backend Integration Tests (`tests/auth_module2.test.js`) and frontend Widget Tests (`test/widget_test.dart`). |
| KPI-010 | Invalid login credentials are rejected | Submit incorrect email or password combinations and verify authentication failure response | HTTP 401 response with code `INVALID_CREDENTIALS` for wrong credentials, or `ACCOUNT_NOT_VERIFIED` / `ACCOUNT_DISABLED` if status restricts login. | HTTP 401 Unauthorized with correct custom error codes returned (e.g. `INVALID_CREDENTIALS`, `ACCOUNT_NOT_VERIFIED`, `ACCOUNT_DISABLED`). | PASS | Checked and validated for incorrect passwords, unregistered emails, pending verification, and disabled statuses. |
| KPI-011 | Authenticated users are redirected to dashboard | Complete successful login and verify dashboard page loads with user-specific journal data | Navigation routing redirects user from `/login` to `/` (dashboard), showing user specific welcome message and digital journal entries. | GoRouter automatically redirects to `/` on successful auth. Dashboard screen renders and greets user: "Hello, Jane Doe" and loads recent entries. | PASS | Verified in frontend widget testing environment (`widget_test.dart`). |
| KPI-012 | Session token is created upon authentication | Verify JWT/session token generation, expiration values, and storage mechanism after login | JWT token is created on the backend. Access token (valid for 15m) and refresh token (valid for 7d) are returned and stored securely on the device (e.g. SharedPreferences). | Backend signs access (15 min) and refresh (7 days) tokens. Frontend stores them under keys `access_token` and `refresh_token` in `SharedPreferences`. | PASS | Verified in backend controllers/services and frontend `auth_repository.dart` implementation. |
| KPI-013 | Expired sessions require re-authentication | Force token expiration and verify protected endpoints deny access until user logs in again | Expired tokens are rejected by the backend with HTTP 401 `TOKEN_EXPIRED`. Frontend intercepts expired state, invalidates session from local storage, and redirects user back to the login screen. | Backend middleware properly rejects expired tokens with 401 `TOKEN_EXPIRED`. The frontend intercepts HTTP 401 Unauthorized responses via Dio interceptor, clears local SharedPreferences, invalidates the active session state in `AuthNotifier`, and automatically routes back to `/login` via `goRouter.go('/login')`. | PASS | Resolved DEF-004 and DEF-005. Tested and verified in client-side flow. |
| KPI-014 | Unsaved journal drafts are preserved during session expiration | Expire user session while editing a journal entry and verify draft content remains recoverable | Draft is written to local storage (SQLite/SharedPreferences) or synced to backend database `Draft` table. If session expires or app is closed, draft is restored upon relaunch/re-auth. | Backend implements `POST /api/v1/drafts` and `GET /api/v1/drafts/:draftId` protected endpoints, storing/retrieving entries from SQLite `Draft` table. Frontend implements auto-save which caches draft data in `SharedPreferences` locally (instant offline resilience) and invokes the remote draft sync API. Displays recovery alert on opening the editor if unsaved drafts are found. | PASS | Resolved DEF-006 and DEF-007. Tested and verified via backend test suite and client auto-save loop. |
| KPI-015 | Password reset request can be initiated | Submit password reset request and verify reset token generation and delivery workflow | Submit email, backend generates a 6-digit code, stores it in `PasswordResetToken` table, logs it in terminal console, and the client navigates to reset verification screen to input it. | Submit email generates 6-digit verification code sent/logged. Client-side screen allows typing the real generated code, which is forwarded directly to the backend `POST /api/v1/auth/reset-password` API and verified successfully. | PASS | Resolved DEF-008. The client-side hardcoded `'123456'` block has been removed. |
| KPI-016 | Expired password reset tokens are rejected | Attempt password reset using expired token and verify request is denied | Reset requests with expired tokens return HTTP 410 `TOKEN_EXPIRED`. | Backend returns 410 `TOKEN_EXPIRED` for expired tokens, which is correctly propagated to the frontend and displayed as a SnackBar error: "Reset failed: Reset token has expired". | PASS | Verified in backend integration tests and frontend password reset flow. |
| KPI-017 | Authentication APIs meet performance requirements | Execute login requests under load and verify average response time remains below 300ms | Average response time remains below 300ms | Average response time per request remains below 300ms (excluding the cold start/setup overhead in testing suites). | PASS | Verified by backend integration test suite time metrics and manual local validation. |
