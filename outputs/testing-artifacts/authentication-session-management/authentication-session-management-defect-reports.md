# Defect Reports - Authentication & Session Management

| Defect ID | Summary | Steps to Reproduce | Expected Result | Actual Result | Environment | Severity | Priority | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **DEF-004** | Frontend fails to redirect user to login on token expiration (KPI-013) | 1. Log in to the application and load the dashboard.<br>2. Wait for the access token to expire (15 minutes), or manually invalidate the active session in the SQLite database by running `UPDATE UserSession SET is_active = 0;`.<br>3. Perform any authenticated action in the app (e.g., refresh the dashboard or view entries). | The app detects the HTTP 401 error, invalidates the local session by deleting tokens from `SharedPreferences`, shows a session expired message, and routes the user back to the `/login` screen. | The API call fails, and a raw error is logged or shown, but the user is kept on the dashboard, and local tokens remain in `SharedPreferences`. | Flutter Client & Node.js Backend | High | High | `api_client.dart` lines 34-37 does not handle 401 or dispatch a logout event. |
| **DEF-005** | Frontend token decoder does not validate expiration claim (KPI-013) | 1. Log in to the application.<br>2. Close the app or reload the browser.<br>3. Wait for the 15-minute access token lifespan to pass.<br>4. Reopen the application. | `getCurrentUser()` detects the expired `exp` claim, removes the expired token, and returns `null` so the app boots to the `/login` screen. | `getCurrentUser()` decodes the token successfully, returns the `User` object, and routes the user to the dashboard, where subsequent API calls fail with 401. | Flutter Client | Medium | Medium | `auth_repository.dart` lines 12-29 decodes the JSON payload but never compares `map['exp']` against the current epoch timestamp. |
| **DEF-006** | Journal draft auto-save is a UI simulation only (KPI-014) | 1. Open the journal editor screen to create a new entry.<br>2. Type some text and wait for the status to show "Draft saved".<br>3. Close the application, refresh the page, or navigate away.<br>4. Return to the editor screen. | The draft content is stored in local persistence (SQLite/SharedPreferences) or backend and is fully recovered when the editor reopens. | The draft content is entirely lost as it was only saved in transient widget memory (RAM). | Flutter Client | High | High | `editor_screen.dart` lines 116-129 only updates the UI state after a delay without performing any storage operations. |
| **DEF-007** | Missing backend endpoints for draft management (KPI-014) | 1. Inspect `authRoutes.js` and other routing configurations.<br>2. Check controllers and services. | Endpoints like `POST /api/v1/drafts`, `GET /api/v1/drafts`, and database interactions are implemented in repositories and controllers. | No endpoints or logic exist. The table is orphaned. | Node.js Backend | Medium | High | `grep_search` results confirm that `Draft` table is only referenced in schema.sql, seeds.sql, and database tests. |
| **DEF-008** | Frontend password reset flow is hardcoded to "123456" and blocks integration (KPI-015 / KPI-016) | 1. Open the Password Recovery screen.<br>2. Submit email to receive a code.<br>3. Check console/logs for the generated code (e.g., `456789`).<br>4. Enter `456789` and the new password. | The app permits submission of the real code to the API, and the backend verifies and updates the password. | The frontend shows a SnackBar: "Invalid code. Try using: 123456" and aborts the API request. If the user enters `123456`, the API request is sent but returns `400 INVALID_TOKEN` from the backend database. | Flutter Client | Critical | Critical | `auth_screens.dart` lines 555-561 contains the client-side interceptor that blocks non-`123456` entries. |
| **DEF-009** | Mobile users are unable to logout (Usability / Session Management) | 1. Run the app in mobile viewport.<br>2. Log in with valid credentials.<br>3. Access the dashboard and settings screens. | A logout button or profile tile is accessible on the Settings screen or on the Dashboard app bar for mobile viewports. | There is no logout button anywhere in the mobile layout, making it impossible to sign out. | Flutter Client | High | High | `main_shell.dart` only shows the logout tile in the desktop sidebar section; `settings_screen.dart` contains no logout actions. |
