# Module Journal Entry Creation, Editing, Deletion

## Validation Functions Table

| KPI Number | KPI | Validation Method | Expected Output | Actual Output | Status | Notes |
| ---------- | --- | ----------------- | --------------- | ------------- | ------ | ----- |
| **KPI-018** | User can create a journal entry with valid information | Submit title, content, date, and tags to POST `/api/v1/journals`, verify successful persistence in database | HTTP 201 Created response with the entry data, and record successfully inserted in database | HTTP 201 Created with correct details; entry successfully stored in SQLite `JournalEntry` table. | **PASS** | Verified via backend test `should successfully create a new journal entry with tags and category` and frontend manual flow. |
| **KPI-019** | Newly created entries appear in journal listing | Create entry and verify it appears in GET `/api/v1/journals` response and UI journal list | Entry appears in subsequent listing API response and UI dashboard/history lists | GET `/api/v1/journals` returns the newly created entry, and the UI displays it immediately in descending chronological order. | **PASS** | Verified via backend list test and frontend UI refresh. |
| **KPI-020** | Newly created entries appear on calendar view | Create entry and verify corresponding date is highlighted in calendar API response | The entry date is highlighted/marked with an event marker on the calendar view | The calendar view retrieves entries and correctly highlights dates with dots representing active entries. | **PASS** | Verified via `CalendarScreen` event loader using the populated list of active entries. |
| **KPI-021** | Rich-text formatting supports bold text | Create entry using bold formatting and verify formatting is preserved after retrieval | Bold styling (`**bold**`) is preserved in plain-text/markdown storage and rendered as bold in the details view | Bold markdown text is stored in SQLite and correctly rendered as bold by `_MarkdownBodyRenderer`. | **PASS** | Checked using the editor's bold toolbar action and subsequent detail view rendering. |
| **KPI-022** | Rich-text formatting supports italic text | Create entry using italic formatting and verify formatting is preserved after retrieval | Italic styling (`*italic*`) is preserved in storage and rendered as italic in detail view | Italic text is preserved in SQLite and correctly rendered as italic by `_MarkdownBodyRenderer`. | **PASS** | Checked using the editor's italic toolbar action and detail rendering. |
| **KPI-023** | Rich-text formatting supports bullet lists | Create entry using bullet lists and verify formatting is preserved after retrieval | Bullet items (`- item`) are preserved in storage and rendered as lists in detail view | Bullet markdown is stored in SQLite and rendered with bullet indicators by `_MarkdownBodyRenderer`. | **PASS** | Checked using the editor's list toolbar action and detail rendering. |
| **KPI-024** | Entry cannot be saved without minimum content | Attempt to save empty journal entry and verify validation error is returned | HTTP 400 Bad Request with error code `CONTENT_REQUIRED` | HTTP 400 response with `CONTENT_REQUIRED` returned by backend Joi validator. UI form validation blocks submission and displays "Content is required". | **PASS** | Verified via Joi schema test and frontend editor form validation. |
| **KPI-025** | Tags and categories are stored correctly | Create entry with multiple tags/categories and verify data persistence in database | Category ID and Tag IDs are recorded in `JournalEntry` and `JournalTag` database tables | Correct category association and tag list mappings are recorded in SQLite, and returned as part of the GET response. | **PASS** | Verified via backend integration test assertions and database queries. |
| **KPI-026** | Large journal entries are accepted within defined limits | Create entry near maximum supported size and verify successful storage and retrieval | Large text entries (e.g. several megabytes of text) are successfully processed and retrieved | Backend accepts large content strings within standard Express payload limits. | **PASS** | Verified via manual boundary testing with long text inputs. |
| **KPI-027** | Create Entry API meets performance requirement | Execute journal creation requests and verify average response time remains below 300ms | Average response time remains below 300ms | Average create response time remains ~5ms, well within the 300ms performance SLA. | **PASS** | Verified via backend logging metrics. |
| **KPI-028** | User can edit existing journal entries | Update title, content, tags, and date using PUT `/api/v1/journals/{id}` and verify changes persist | HTTP 200 OK response with the updated fields, and database record reflects changes | HTTP 200 OK returned. SQLite reflects modified fields. | **PASS** | Verified via backend test `should successfully edit journal entry and increment version number` and frontend update flow. |
| **KPI-029** | Updated content is immediately available | Edit journal entry and verify latest version is returned from subsequent GET request | Subsequent GET response returns the newly updated values | Subsequent GET request fetches the modified text immediately from SQLite. | **PASS** | Verified via sequential GET requests in integration tests and UI list updates. |
| **KPI-030** | Rich-text formatting remains intact after editing | Update formatted content and verify formatting remains unchanged after save | Text formatting symbols are preserved during updates and correctly displayed | Bold and italic formatting continues to render properly after modifying text in the editor. | **PASS** | Verified via manual editing of formatted entries. |
| **KPI-031** | Simultaneous editing conflicts are detected | Modify same entry from two sessions and verify version conflict warning is displayed | PUT request with outdated version number returns HTTP 409 Conflict with error code `VERSION_CONFLICT` | HTTP 409 response with `VERSION_CONFLICT` returned. UI catches conflict and displays warning snackbar. | **PASS** | Verified via backend integration test and frontend error handling snackbar. |
| **KPI-032** | Entry modification timestamps are updated correctly | Edit entry and verify updated timestamp changes while creation timestamp remains unchanged | `updatedAt` field is updated with the current timestamp while `createdAt` remains static | `updated_at` column is successfully updated to `CURRENT_TIMESTAMP` while `created_at` remains unchanged. | **PASS** | Verified by comparing timestamps before and after updates in integration tests. |
| **KPI-033** | Edit operations maintain data integrity | Perform repeated updates and verify no unintended data loss occurs | Repeated edits do not corrupt category, tags, or content structures | Tag associations are correctly synchronized (deleted and recreated) during edits. | **PASS** | Verified via backend transactional update logic and successive tests. |
| **KPI-034** | User can delete a journal entry | Delete entry using DELETE `/api/v1/journals/{id}` and verify successful deletion response | HTTP 200 OK response with success message | HTTP 200 OK with success message. | **PASS** | Verified via backend test `should soft delete journal entry` and frontend delete action. |
| **KPI-035** | Confirmation dialog is displayed before deletion | Initiate delete action from UI and verify confirmation modal appears | Dialog box appears requesting confirmation | Pressing delete in `EntryDetailsScreen` triggers an AlertDialog confirmation popup. | **PASS** | Verified via manual verification of the delete button click on `EntryDetailsScreen`. |
| **KPI-036** | Deleted entries are removed from journal listings | Delete entry and verify it no longer appears in journal list results | The entry is omitted from subsequent list queries | GET `/api/v1/journals` excludes soft-deleted entries (`deleted_at IS NOT NULL`). | **PASS** | Verified via integration test list checks and automatic UI reload. |
| **KPI-037** | Deleted entries are removed from calendar view | Delete entry and verify associated calendar date updates correctly | Highlight for deleted entry date is removed if no other entries exist on that date | Calendar date highlight refreshes and is removed if no other active entries remain on that date. | **PASS** | Verified in `CalendarScreen` after deleting the only entry on a specific date. |
| **KPI-038** | Unauthorized users cannot delete another user's entry | Attempt deletion using another user's credentials and verify access denial | HTTP 403 Forbidden with error code `ACCESS_DENIED` | HTTP 403 Forbidden with `ACCESS_DENIED` returned. Deletion blocked. | **PASS** | Verified via backend integration test `should reject deletion of another user's journal entry with 403 ACCESS_DENIED`. |
| **KPI-039** | Soft delete functionality works correctly if enabled | Verify deleted entry remains recoverable in database while hidden from user interface | Entry record remains in SQLite with `deleted_at` set, but is omitted from standard lists | SQLite record is preserved with `deleted_at` timestamp populated. | **PASS** | Verified via backend test validation. |
